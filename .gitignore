# -----------------------------------------------------------------------------
# 模型权重 & 微调产物：只保留在实验机，不进 git
# -----------------------------------------------------------------------------
models/
# 整个 results/ 目录都用于 LoRA adapter 权重（几百 MB 级），一律不追踪
results/

# -----------------------------------------------------------------------------
# data/ 分层：只忽略真正的大文件（KG 二进制/CSV），其它一律进 git
# -----------------------------------------------------------------------------
# 说明：data/raw/*.csv 单文件 <10MB，整体 <20MB，纳入 git 便于一键复现整个数据流。
#      DDIcorpus 变化频率极低，进 git 后实验机 clone 就能跑数据构造。

# PrimeKG 大文件（kg.csv 数百 MB / primekg_graph.pkl 数百 MB / primekg_pyg.pt 数百 MB）
data/kg/*.csv
data/kg/*.pkl
data/kg/*.pt
# drug_name_map.json 小文件，保留追踪（用于药名对齐可复现）

# -----------------------------------------------------------------------------
# Python 常规忽略
# -----------------------------------------------------------------------------
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# 训练 offload 目录（transformers 分片时会创建）
offload/

# -----------------------------------------------------------------------------
# 编辑器 / 系统
# -----------------------------------------------------------------------------
.idea/
.vscode/
.DS_Store

# -----------------------------------------------------------------------------
# WorkBuddy 助手本地记忆/配置目录，非项目代码
# -----------------------------------------------------------------------------
.workbuddy/

# -----------------------------------------------------------------------------
# 本地凭证（GitHub PAT 等），永远不追踪
# -----------------------------------------------------------------------------
.git/credentials
.env
.env.*
!.env.example
secrets/

# 本地展示与临时生成物，不参与两台实验机器的数据交接
output/
tmp/
