# -----------------------------------------------------------------------------
# 模型权重 & 微调产物：只保留在实验机，不进 git
# -----------------------------------------------------------------------------
models/
# 整个 results/ 目录都用于 LoRA adapter 权重（几百 MB 级），一律不追踪
results/

# -----------------------------------------------------------------------------
# data/ 分层：源数据与大 KG 文件不进 git，其余（labels/finetune/reports）追踪
# -----------------------------------------------------------------------------
# 源 DDIcorpus CSV（数百 MB / 数十 MB 级），只在实验机本地
data/raw/*.csv

# PrimeKG 大文件（kg.csv 数百 MB / graph.pkl 数百 MB / pyg.pt 数百 MB）
data/kg/*.csv
data/kg/*.pkl
data/kg/*.pt
# drug_name_map.json 小文件，保留追踪（用于药名对齐可复现）

# -----------------------------------------------------------------------------
# Python 常规忽略
# -----------------------------------------------------------------------------
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# 训练 offload 目录（transformers 分片时会创建）
offload/

# -----------------------------------------------------------------------------
# 编辑器 / 系统
# -----------------------------------------------------------------------------
.idea/
.vscode/
.DS_Store

# -----------------------------------------------------------------------------
# WorkBuddy 助手本地记忆/配置目录，非项目代码
# -----------------------------------------------------------------------------
.workbuddy/

# -----------------------------------------------------------------------------
# 本地凭证（GitHub PAT 等），永远不追踪
# -----------------------------------------------------------------------------
.git/credentials
